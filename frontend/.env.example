# Copie para .env.local e preencha os valores reais.
# NUNCA commite .env.local — ele já está no .gitignore.

# URL do backend (HF Spaces)
NEXT_PUBLIC_API_URL=https://<seu-usuario>-baja-paint-inspection-api.hf.space

# Supabase — somente leitura (anon key — pode ficar no frontend)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
